import NodePieces from "../nodePieces";

export default class Horse {
    initial = new NodePieces(0, 1);
    constructor() {
    }

    move(x, y) {
        // const move = this.initial.mv(x, y);
        if (this.initial.ct[0] === 0) {
            if (x == current[0] + 2 &&
                current[1] - 1 ||
                current[1] + 1 &&
                x !== -1 && y !== -1) {

            }
        }

    }

}